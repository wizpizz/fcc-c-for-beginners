#include <stdio.h>
#include <stdlib.h>

int working_with_numbers()
{
    int num = 6;

    printf("%f", floor(36.656));
    // + addition
    // - subtraction
    // * multiplication
    // / division

    // int + int = int
    // int + float = int

    // pow() power to the
    // sqrt() square root
    // ceil() round up
    // floor() round down


    return 0;
}
