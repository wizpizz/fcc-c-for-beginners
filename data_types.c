#include <stdio.h>
#include <stdlib.h>

int data_types()
{
    int age = 40; // an integer, a whole number
    double gpa = 3.6; // a double, a decimal number
    char grade = 'A'; // a character, stored in single quotes
    char phrase[] = "Giraffe Academy"; // a string, an array of characters


    return 0;
}
